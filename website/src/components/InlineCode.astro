---
interface Props {
  class?: string;
}

const { class: extra } = Astro.props;
---

<code
  class:list={[
    'rounded bg-slate-100 px-1.5 py-0.5 font-mono text-[0.9em] text-slate-700',
    extra,
  ]}
  ><slot /></code
>
